// Script de montaje FFmpeg para el pipeline v4 de JotaTiger Fit
// Ver PLAN-V4.md para diseño completo y fases de implementación.

const config = {
  // inputAvatar: '',      // avatar.mp4 (HeyGen, fondo verde)
  // inputBrolls: [],      // array de { path, start_seconds, end_seconds }
  // inputLogo: '',        // assets/jotatiger-logo.png
  // titleText: '',        // texto del título principal
  // titleAssPath: '',     // .ass generado en tiempo de ejecución
  // format: 'horizontal', // 'horizontal' | 'vertical'
  // outputPath: '',       // ruta del MP4 final
};

async function buildVideo(config) {
  console.log('build-video.js: pipeline v4 pendiente de implementar');
}

module.exports = { buildVideo };
